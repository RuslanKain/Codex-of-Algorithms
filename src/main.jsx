import React from 'react'
import { createRoot } from 'react-dom/client'
import CodexAlgo from '../codex-of-algorithms.jsx'

createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <CodexAlgo />
  </React.StrictMode>
)
